package com.ganesh.smartwatermeter.common.repository.db;


/*
@Database(entities = {MembersModel.class}, version = 1, exportSchema = false)
public abstract class SWMDatabase extends RoomDatabase {

    private static volatile SWMDatabase INSTANCE;

    public static SWMDatabase getDatabase() {
        if (INSTANCE == null) {
            synchronized (SWMDatabase.class) {
                if (INSTANCE == null) {
                    INSTANCE = Room.databaseBuilder(App.getContext(), SWMDatabase.class, "mmt_db")
                            .build();
                }
            }
        }
        return INSTANCE;
    }

    public abstract MemberDao memberDao();


}*/
